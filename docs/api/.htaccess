# Папку с накопленными отметками наружу не отдаём: она нужна только PHP.
<IfModule mod_authz_core.c>
  <FilesMatch "\.json$">
    Require all denied
  </FilesMatch>
</IfModule>
<IfModule !mod_authz_core.c>
  <FilesMatch "\.json$">
    Order allow,deny
    Deny from all
  </FilesMatch>
</IfModule>
